{
  "name": "fb_mockapi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch data/db.json --port 8488"
  },
  "author": "tran.duc.linhz",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.2"
  }
}
